### 🎙️ WhoVoide项目计划书

#### 第一阶段：数据获取与标准化构建 (Data & Preprocessing)

爬取音频是最容易陷入脏数据泥潭的一步。明星的语音通常伴随背景音乐、采访杂音或其他人的声音，因此清洗环节至关重要。

- **1. 基础语料获取：**
  - 除了自己写爬虫定向抓取特定的访谈节目或播客外，强烈建议先利用现成的开源数据集（如 **VoxCeleb** 1 & 2）作为基础底座。VoxCeleb 本身就包含了大量名人的纯净语音，可以极大地减少前期的爬虫工作量。
- **2. 语音清洗与 VAD（端点检测）：**
  - 使用 Voice Activity Detection (VAD) 工具（如 WebRTC VAD 或 Silero VAD）剔除音频中的静音段。
  - 引入音频分离模型（如 Spleeter 或 Demucs）将人声与背景BGM剥离，确保提取的是纯净的人声特征。
- **3. 音频标准化：**
  - 将所有切片统一格式化（例如：单声道，16kHz 采样率，16-bit PCM 编码），切割为 5-10 秒的短语音片段，方便后续批量输入模型。

#### 第二阶段：声纹特征提取与向量化 (Embedding Extraction)

这是整个项目最核心的算法模块，决定了“像不像”的准确度。我们需要将变长的语音片段转化为固定长度的特征向量（Speaker Embedding）。

- **1. 模型选型：**
  - **经典高性能基线：** 推荐使用 **ECAPA-TDNN**，它在捕获说话人全局特征方面表现极其出色，且推理速度快，非常适合作为工程部署的基线模型。
    https://github.com/yeyupiaoling/VoiceprintRecognition-Pytorch
- **2. 向量库建立：**
  - 将清洗好的明星语音库批量输入模型，提取出 192 维或 512 维的声纹向量。
  - 对同一个明星的多个语音片段向量求平均，或者进行聚类，得到该明星的“标准声纹质心向量”。

#### 第三阶段：后端工程与大规模向量检索 (Backend & Search)

传统的关系型数据库不适合做高维向量的余弦相似度计算，需要引入专业的向量检索引擎。

- **1. 核心后端框架：**
  - 推荐使用 **Django 4.0**。它成熟稳健，能够很好地处理复杂的业务逻辑、模型加载以及路由分发，同时自带的 ORM 能高效管理元数据。
- **2. 数据库设计：**
  - **关系型数据 (MySQL 8.0)：** 存储明星的基础信息（姓名、头像、简介）、用户注册认证信息以及历史匹配记录。
  - **向量数据库 (FAISS 或 Milvus)：** 将明星的声纹向量导入向量库。当用户上传音频并生成向量后，通过向量库计算余弦相似度（Cosine Similarity），快速召回 Top-N 最相似的明星。

#### 第四阶段：前端交互与系统串联 (Frontend Development)

前端主要负责采集用户的实时语音并提供流畅的交互反馈。

- **1. 前端框架：**
  - 采用 **Vue.js 3.0** 构建响应式页面。利用其组合式 API (Composition API) 可以很优雅地封装录音组件和状态管理。
- **2. 录音与实时传输：**
  - 使用浏览器的 Web Audio API 实现网页端实时录音。
  - 为了提升用户体验，可以在前端增加一个简单的音频波形可视化组件，录制完成后通过 Axios 将音频 Blob 数据作为表单文件发送给 Django 后端。
- **3. 结果展示：**
  - 前端接收到后端返回的 JSON 匹配结果后，展示匹配度最高的明星卡片、相似度百分比，以及该明星的一段经典语音供用户对比试听。
