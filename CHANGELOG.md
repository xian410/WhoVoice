# WhoVoice v0.0.2 版本说明

## 版本信息

- **版本号**: v0.0.2
- **发布日期**: 2026-06-22
- **基于**: v0.0.1

---

## v0.0.2 新增功能

### 1. 声纹库重建（核心重构）

- **FAISS 索引重建** — 从零重建包含 **421 位歌手** 的声纹索引（原为 443 位，移除 22 位无音频歌手）
- **确定性顺序 ID** — 废弃不稳定的 Python `hash()`，改用顺序整数 ID，消除跨进程匹配失败问题
- **GPU 分批提取** — 每处理 30 位歌手卸载模型并清理显存，彻底解决 GPU OOM
- **增量 npy 缓存** — 提取的 embedding 先存磁盘，全部完成后 CPU 建索引，避免显存累积

### 2. 声纹匹配 API 增强

- **`total_celebrities` 字段** — 匹配响应新增实际注册数，修复前端显示"仅注册 5 位明星"的错误
- **`has_audio` 字段** — 标记每位匹配结果是否有可试听音频，无音频歌手自动排底
- **海报数据修复** — 上传音频前先用 ffmpeg 转码为 WAV，解决 librosa 对 WebM 格式不兼容导致海报分析失败的问题

### 3. 试听功能优化

- **副歌检测算法重写** — 四级检测策略：
  - 策略1：全局最佳能量跃升比（取代原来的"第一个达标"）
  - 策略1b：全局峰值回溯
  - 策略1c：高能跳转（整首歌一直响的歌曲跳到 37s）
  - 回退：人声起始检测
- **慢歌/抒情歌优化** — 降低跃升比阈值（1.4 → 1.3），捕获更细微的副歌过渡
- **阈值分析验证** — 经 6 位歌手 16 首歌曲实测验证

### 4. 测试与评估工具

| 脚本 | 功能 |
|------|------|
| `scripts/evaluate_model.py` | 声纹模型评估（Top-K 准确率、EER、正负样本分析） |
| `scripts/extract_test_samples.py` | 提取歌手副歌切片到 `data/test_samples/` |
| `scripts/sync_celebrity_info.py` | 同步 celebrities_info.json 与 FAISS 元数据 |

### 5. 前端改进

- **匹配结果卡片** — 试听/详情双按钮设计（取消整卡点击跳转）
- **试听按钮** — 有音频显示 🎧 试听，无音频显示 🔇 无试听
- **歌手详情页** — 优化布局，新增 B 站图标跳转
- **FAISS 可视化** — 新增 `/api/voice-matching/faiss-visualizer/` 散点图/聚类图页面

### 6. 其他

- 分享文案末尾的 `->` 改为实际链接 `https://whovoice.online/`
- 模块级 ffmpeg 共享函数，消除 `AudioSampleView` 中的重复代码
- `.gitignore` 更新，忽略测试数据和构建缓存

---

# WhoVoice v0.0.3 版本说明

## 版本信息

- **版本号**: v0.0.3
- **发布日期**: 2026-06-22
- **基于**: v0.0.2

---

## v0.0.3 新增功能

### 1. 纯净人声声纹库（流程2）

- **Demucs 分离伴奏** — 对全部 421 位歌手的原始音频运行 Demucs 分离伴奏，提取纯净人声
- **轻量化 8GB 显存优化** — Demucs 子进程分离 + CAM++ 串行推理，逐歌手清理显存，零 OOM
- **副歌 15s 截取** — 配合副歌检测算法，只保留有效人声片段
- **420 位歌手入库**（PSY 韩语文件名 Demucs 解析失败）
- **双索引并存** — 带伴奏（421 位）和纯净人声（420 位）两个 FAISS 索引同时可用

### 2. 声纹库前端切换

- **切换按钮** — 页面底部新增声纹库选择器，一键切换 🎵 带伴奏 / 🎤 纯净人声
- **默认纯净人声** — 页面打开默认选中纯净人声索引
- **实时更新** — 切换后匹配结果和歌手数量即时刷新

### 3. 页脚优化

- **布局重构** — 声纹库切换、版本号、作者信息分层展示
- **版本弹窗** — 点击 v0.0.2 弹出迭代记录
- **作者信息** — 页脚显示 ljx 和联系方式

---

## 性能指标（v0.0.3）

| 指标 | 带伴奏索引 | 纯净人声索引 |
|------|-----------|-------------|
| 已注册歌手 | 421 位 | 420 位 |
| 数据源 | data/raw/ 原始音频 | Demucs 分离后纯人声 |
| 构建方式 | GPU 分批提取 | 子进程串行（防 OOM） |
| 默认启用 | ❌ | ✅ |
